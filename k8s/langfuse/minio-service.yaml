apiVersion: v1
kind: Service
metadata:
  name: langfuse-minio
  namespace: investigator-ai
spec:
  type: ClusterIP
  selector:
    app: langfuse-minio
  ports:
    - name: s3
      port: 9000
      targetPort: 9000
    - name: console
      port: 9001
      targetPort: 9001
